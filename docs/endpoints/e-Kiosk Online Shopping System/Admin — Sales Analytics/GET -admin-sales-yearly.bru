meta {
  name: GET /admin/sales/yearly
  type: http
  seq: 5
}

get {
  url: {{baseUrl}}/admin/sales/yearly
  body: none
  auth: inherit
}

headers {
  Cookie: JSESSIONID={{sessionId}}
}

settings {
  encodeUrl: true
}

docs {
  Revenue total for rolling last-365-days window.
  Access: ADMIN
}
