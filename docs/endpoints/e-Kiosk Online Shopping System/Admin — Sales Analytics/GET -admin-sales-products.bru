meta {
  name: GET /admin/sales/products
  type: http
  seq: 6
}

get {
  url: {{baseUrl}}/admin/sales/products
  body: none
  auth: inherit
}

headers {
  Cookie: JSESSIONID={{sessionId}}
}

settings {
  encodeUrl: true
}

docs {
  Fast-moving (top 10 by units sold) and slow-moving (bottom 10 + never-ordered) product rankings.
  Access: ADMIN
}
