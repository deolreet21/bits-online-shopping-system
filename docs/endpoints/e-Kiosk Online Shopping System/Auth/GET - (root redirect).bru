meta {
  name: GET / (root redirect)
  type: http
  seq: 1
}

get {
  url: {{baseUrl}}/
  body: none
  auth: inherit
}

settings {
  encodeUrl: true
}

docs {
  Root redirect. No session → /login. ADMIN → /admin/dashboard. CUSTOMER → /customer/dashboard.
  Access: PUBLIC
}
