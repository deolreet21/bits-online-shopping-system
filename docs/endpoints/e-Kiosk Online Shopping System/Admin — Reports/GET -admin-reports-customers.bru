meta {
  name: GET /admin/reports/customers
  type: http
  seq: 5
}

get {
  url: {{baseUrl}}/admin/reports/customers
  body: none
  auth: inherit
}

headers {
  Cookie: JSESSIONID={{sessionId}}
}

settings {
  encodeUrl: true
}

docs {
  All CUSTOMER accounts with their order count. Shows totalCustomers and activeCustomers (those with at least 1 order).
  Access: ADMIN
}
